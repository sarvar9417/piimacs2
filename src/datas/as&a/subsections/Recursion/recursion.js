import {recursions} from './recursions'

export const recursion = {
    name: 'Recursion',
    routes: [
        {
            name: 'Recursion', to:'/recursion', datas: recursions
        }
    ]

}
